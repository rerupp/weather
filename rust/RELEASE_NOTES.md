# Release Notes

* Changed the structure of the Rust weather projects
    * moved the weather workspace up one level
    * added toolslib into the workspace.
    * Renamed the following directories:
        * tui into tui_lib
        * termui into termui_lib
        * weather into weather_lib
        * py_lib into py_weather_lib
* Changed backend::filesys FsAdmin::details() to use a ArchivesIterator
* Changed the database schema adding a `dates` table. The `metadata` table was
  modified to store only filesystem store properties.
* The `ls` user command was updated to include detailed information concerning
  the filesystem and database storage.
* The `weather_lib::backend::db::sqlite` module was revamped separating the
  weather history and cities database into separate modules. A `tables` module
  has been added providing an abstraction layer over the database schema (I hate
  ORMs).
* The toolchain has been updated to 1.96.0.

## May 2025

This release includes changes to the CLI frontend and backend. Here is a summary of the changes.

* The Cities database was changed to include support for multiple countries. The release includes
  both US and Canadian city databases.
* The location metadata was updated to include country and region proprerties.
* A new lightweight TUI library was added superseding the original Rust TUI library. The TUI now
  runs in a viewport instead of using the alternate screen.
* The list cities CLI command was changed to reflect the country and region updates. A
  TUI option was added allowing interactive query of the database.
* The add and modify location CLI command now have a TUI option allowing changes to be made
  interactively.
* The filesystem backend history archives iterator was replaced with a new version.
* A new admin <code>compress</code> command was added to reclaim disk space used by history
  archives.
* The filesystem backend was changed to provide cleaner access to filesystem operations from the
  database.
* The <code>LocationFilter</code> was updated to separate finding locations by city name or
  alias name. Finding locations by country or region names is also included.

## Prior Releases

### Dec 2025

This release has seen a lot of changes to the weather data backend. Here is a summary of the
changes:

* The weather data configuration properties have gone through a major overhaul. A **config**
  command as added to the administration CLI to provide basic support.
* `WeatherData` has full CRUD operations for a location.
* The administration command stack went through a major overhaul. The `FsAdmin` API was
  introduced to support filesystem administration needs. The `DbAdmin` API was introduced to
  support database administrative needs.
* Updated the accepted date formats that can be used with the CLI.
* A new administration **copy** command was added that copies the properties and histories
  of an existing location.
* A new administration **check** command was added that verifies the filesystem storage and
  the database contents match.
* Added a new threading infrastructure, to the filesystem backend, allowing history archives to
  be read in parallel.
* The filesystem backend was changed to use the new threading infrastructure for bulk queries
  such as location history dates.

### Oct 2025

This release replaces the previous HTTP client implementation that was used to get historical
weather data. A new [WeatherData](weather_lib/src/weather_data.rs) API call was added that returns a
[HistoriesFuture](weather_lib/src/histories_future.rs). The `WeatherData` API is not `async` and in
most cases it is not an issue since response times are typically under 10ms. Getting
historical weather data is different because it comes from a remote server. The *future*
implementation uses a background thread to collect the data and manage the thread being
used.

A Python [type stubs](py_weather_lib/py.typed) file has been added to the
[py_lib](py_weather_lib/README.md) project. While I'm not pleased having to maintain a separate file
it is worth it to have PyCharm grok the `weather_data` API and associated entities.
It would be great if the *type stubs* file could be autogenerated when building the
`py_weather_lib` Python package but that doesn't seem to be happening any time soon.

### Sep 2025

This is a copy of the latest version in the `rust_playground`. The `rust_playground` will
continue to live on but weather history code will no longer be changed there.
